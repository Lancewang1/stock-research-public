# 金融垂类模型数据工程设计框架

> 文档状态：总体设计 v1.0
> 适用范围：宏观、公司基本面、价量、新闻、卖方研报、管理层访谈及其他金融语料
> 本文边界：覆盖数据进入系统到形成可训练数据集，不展开模型训练、评估与线上推理架构

## 1. 目标与原则

本系统的目标不是建立一个“文件仓库”，而是建立一条可重复、可审计、可检索、可授权、可版本化的金融数据生产线。员工同一天提交十份不同类型的文件，或系统每天接收数百万条行情记录时，二者都应进入同一套标准化控制体系，并最终编译成用途明确、来源可追溯的数据集。

总体流程：

```text
外部文件 / API / 音频 / 数据库
              │
              ▼
       1. DATA INTAKE
       接收、登记、初步分类
              │
              ▼
       2. DATA PROCESSING
       解析、清洗、标准化、切分
              │
              ▼
       3. DATA ANNOTATION
       事实、语义、关系、证据标注
              │
              ▼
  4. DATASET CURATION & COMPILATION
       筛选、采样、编译、切分、发布
              │
              ▼
       版本化训练 / 评估数据集
```

核心原则：

1. **原始数据不可变**：原文件、原始 API 响应和原始音频只追加、不覆盖；修订版是新资产。
2. **来源先于内容**：每个对象必须知道来源、授权、发布时间、获取时间和证据位置。
3. **事实与观点分离**：公司披露事实、管理层指引、分析师预测、市场一致预期和分析师观点是不同对象，不能互相覆盖。
4. **时间可回放**：任何历史训练或回测只能看到当时已经可获得的数据，避免前视偏差。
5. **机器预处理、人类确认、专家仲裁**：自动化承担规模，人类承担关键判断，金融专家处理冲突和高风险标签。
6. **存储与检索分离**：物理路径服务于稳定存储；业务检索依靠资产目录和索引，不靠员工记文件夹。
7. **数据集是编译产物**：模型不直接读取生产数据库；训练数据由固定规则从版本化快照编译生成。
8. **全链路可追溯**：训练样本必须能回溯到标注、处理结果、原始文件及原始证据。

## 2. 四层数据对象

| 层级 | 对象 | 含义 | 是否可修改 | 典型存储 |
|---|---|---|---|---|
| L0 | RAW DATA | 原文件、原始音频、原始 API 响应 | 否，只能新增版本 | S3 / MinIO 对象存储 |
| L1 | PROCESSED DATA | 解析文本、表格、转录、标准化时序数据、文本块 | 原结果不覆盖，重跑生成新版本 | 对象存储 + Lakehouse |
| L2 | ANNOTATED / CURATED DATA | 经审核的实体、指标、事件、语义和关系标注 | 标签版本化，保留历史 | PostgreSQL + Lakehouse |
| L3 | DATASET ARTIFACTS | CPT、SFT、推理、工具调用、偏好和评估数据集 | 不可变版本 | 对象存储 + Dataset Registry |

“CURATED DATA”不是另一份手工复制的文件，而是通过质量门槛的有效标注与处理对象形成的逻辑视图或版本化快照。

## 3. 数据类型与分类体系

### 3.1 一级数据家族

系统至少覆盖以下 12 个一级家族；它们是业务分类，不是物理文件夹。

| 编号 | 数据家族 | 典型内容 | 典型来源 | 主要时间轴 |
|---|---|---|---|---|
| D01 | 宏观经济 | GDP、CPI、就业、PMI、利率、流动性 | 统计局、央行、政府机构、授权供应商 | 观察期、首次发布、修订发布 |
| D02 | 公司监管披露 | 10-K、10-Q、8-K、年报、公告、招股书 | SEC、交易所、公司 IR | 报告期、提交、公开可用 |
| D03 | 公司财务与经营指标 | 三张表、分部收入、销量、客户结构、KPI | 从监管披露或公司材料抽取 | 财政期、披露时间 |
| D04 | 管理层沟通 | Earnings call、业绩会、投资者日、访谈 | 公司 IR、授权转录/音频供应商 | 事件开始、发布、可用 |
| D05 | 管理层指引 | 收入、毛利率、费用、Capex 指引 | 公司披露、业绩会 | 指引目标期、发布时间 |
| D06 | 卖方研究 | 公司研报、行业研报、宏观研报、策略研报 | 授权券商或研究平台 | 发布、修订、预测期 |
| D07 | 市场一致预期 | 指标共识、评级、目标价及修订 | 授权数据供应商 | 快照时间、预测期 |
| D08 | 新闻与事件 | 公司、行业、政策、地缘、供应链新闻 | 授权媒体、通讯社、公告源 | 事件时间、发布时间、首次发现 |
| D09 | 行情与交易 | OHLCV、逐笔、盘口、公司行动调整因子 | 交易所、授权行情商、券商 API | 交易所事件时间、接收时间 |
| D10 | 衍生品与另类市场 | 期权链、IV、期货曲线、信用利差 | 交易所、授权供应商 | 报价时间、到期日 |
| D11 | 另类数据 | 卫星、招聘、网页、供应链、应用数据 | 合法且授权的数据商 | 观察时间、采集时间 |
| D12 | 内部研究与反馈 | 内部纪要、模型答案、审核、用户反馈 | 内部系统 | 创建、审核、反馈时间 |

### 3.2 多维分类表

单个数据资产必须按多个维度同时分类。上传人只确认业务必需字段；其余字段由规则、连接器和分类模型预填。

| 维度 | 字段示例 | 典型枚举或格式 | 是否必填 |
|---|---|---|---|
| 1. 输入通道 | `ingest_channel` | WEB_UPLOAD、API、SFTP、EMAIL、CRAWLER、DB_SYNC | 是 |
| 2. 介质类型 | `media_type` | DOCUMENT、AUDIO、VIDEO、TABULAR、TIMESERIES、MESSAGE | 是 |
| 3. 文件格式 | `file_format` | PDF、HTML、DOCX、XLSX、CSV、JSON、XML、XBRL、MP3 | 是 |
| 4. 数据家族 | `data_family` | D01-D12 | 是 |
| 5. 文档/数据集类型 | `document_type` | 10-K、10-Q、EARNINGS_RELEASE、CALL_TRANSCRIPT、COMPANY_RESEARCH、INDUSTRY_RESEARCH 等 | 是 |
| 6. 内容角色 | `content_role` | REPORTED_FACT、MANAGEMENT_COMMENTARY、MANAGEMENT_GUIDANCE、ANALYST_FORECAST、ANALYST_OPINION、CONSENSUS_EXPECTATION | 处理后必填 |
| 7. 主体 | `entity_ids` | 公司、政府、央行、人物的内部规范 ID | 适用时必填 |
| 8. 资产 | `instrument_ids` | ISIN、FIGI、交易所代码及内部资产 ID | 适用时必填 |
| 9. 资产类别 | `asset_class` | EQUITY、BOND、FX、COMMODITY、OPTION、FUTURE、MACRO | 是 |
| 10. 地域 | `geography` | 国家、地区、交易所 | 是 |
| 11. 行业 | `industry` | GICS/自有行业树及版本 | 适用时必填 |
| 12. 主题 | `topics` | AI、数据中心、半导体、利率、供应链等受控词表 | 否 |
| 13. 事件 | `event_type/event_id` | EARNINGS、FOMC、M&A、PRODUCT_LAUNCH 等 | 适用时必填 |
| 14. 财政/观察期 | `period_start/end/fiscal_period` | FY2026Q1、日期区间 | 适用时必填 |
| 15. 时间与时区 | 第 5 节字段组 | ISO 8601 + IANA timezone + precision | 是 |
| 16. 来源等级 | `source_authority` | PRIMARY、LICENSED_SECONDARY、INTERNAL_DERIVED | 是 |
| 17. 授权状态 | `license_policy` | TRAIN_ALLOWED、RAG_ONLY、EVAL_ONLY、INTERNAL_ONLY、EXPIRED | 是 |
| 18. 生命周期 | `processing_state` | RECEIVED、QUARANTINED、PROCESSED、REVIEWED、APPROVED、REJECTED | 是 |
| 19. 语言 | `language` | BCP 47，如 en-US、zh-CN | 是 |
| 20. 敏感等级 | `sensitivity` | PUBLIC、LICENSED、CONFIDENTIAL、RESTRICTED | 是 |
| 21. 质量等级 | `quality_tier` | A、B、C、REJECT | 处理后必填 |
| 22. 预期用途 | `allowed_uses` | CPT、SFT、RAG、EVAL、TOOL、HUMAN_REVIEW | 是 |

### 3.3 来源优先级与引用标准

系统不能通过普通网页搜索一个数字后就写入“事实”。不同内容角色应使用不同权威来源：

| 要表达的内容 | 首选来源 | 次选来源 | 禁止做法 |
|---|---|---|---|
| 已报告 Revenue / Net Income | 监管披露、公司正式财报 | 公司 IR 正式材料 | 用研报预测或搜索摘要冒充已报告事实 |
| 管理层原话 | 公司发布音频/Transcript | 有授权且可核验的转录服务 | 无证据地改写成直接引语 |
| 管理层指引 | 财报、业绩会、公司正式演示 | 可靠转录且回链原事件 | 把分析师预测标为管理层指引 |
| 分析师预测/观点 | 对应券商研报原文 | 授权研报数据库 | 只存二手新闻转述而丢失原研报 |
| 一致预期 | 指定授权供应商的时间点快照 | 经批准的替代供应商 | 混合不同时间快照后称为同一共识 |
| 行情成交 | 交易所或授权行情商 | 经批准的券商 API | 用网页显示价替代可审计行情序列 |
| 宏观数据 | 官方统计/央行及其 vintages | 授权宏观供应商 | 用最新修订值回填历史首次发布值 |

同一事实可以有多个来源，但必须分别保存 observation，并建立 `SUPPORTS`、`CONTRADICTS` 或 `SUPERSEDES` 关系；不能静默覆盖。

## 4. DATA INTAKE：接收与资产登记

### 4.1 输入方式

建设统一的 **Intake & Classification Engine**，包含四类入口：

1. **员工上传门户**：支持批量拖拽文件，机器预识别，员工确认少量关键字段。
2. **API Connector**：按配置定时拉取行情、宏观、公告、新闻等数据，自动携带供应商和授权信息。
3. **批量交付入口**：SFTP、对象存储 drop zone、数据库同步，适合供应商大批量文件。
4. **事件驱动入口**：消息队列或 webhook，适合实时新闻和行情。

员工一次上传十份文件时，界面流程为：

```text
拖拽 10 份文件
   → 病毒/安全扫描、哈希查重
   → OCR/文件名/首页快速识别
   → 系统建议文档类型、公司、期间、来源
   → 员工在批量表格中确认或纠正
   → 逐项显示授权和必填项校验
   → 提交为一个 batch_id
```

员工必须确认：`document_type`、`source_org`、`source_publish_time`（及精度）、`license_policy`、主要实体/资产、语言。系统生成：`asset_id`、哈希、接收时间、文件格式、上传人、批次、存储 URI 和初步质量状态。

API 数据由连接器配置预先声明来源、授权、schema、频率和时区，日常运行不要求员工逐条确认；异常批次进入隔离队列。

### 4.2 RAW DATA 存储

RAW DATA 物理路径按接收日期和技术 ID 组织，不按公司或文档类别建业务文件夹：

```text
s3://raw-data/
  ingest_date=2026-08-05/
    batch_id=bat_01J.../
      asset_id=ast_01J.../
        original.pdf
        envelope.json
```

`envelope.json` 保存传输层元数据；完整业务元数据写入 Data Asset Catalog。推荐启用对象锁、版本控制、服务端加密和校验和。文件重复时保留一次物理对象，并记录多个接收事件；内容修订时生成新 `asset_id` 并通过 `SUPERSEDES` 关联旧版。

### 4.3 Data Asset Catalog 最小字段

```json
{
  "asset_id": "ast_01J...",
  "batch_id": "bat_01J...",
  "title": "NVIDIA FY2026 Q1 Earnings Release",
  "document_type": "EARNINGS_RELEASE",
  "data_family": "D02",
  "source_org_id": "org_nvda_ir",
  "source_uri": "https://...",
  "source_publish_time": "2025-05-28T13:00:00-07:00",
  "timestamp_precision": "SECOND",
  "first_seen_time": "2025-05-28T13:00:08-07:00",
  "ingest_time": "2026-08-05T10:15:31+08:00",
  "available_time": "2025-05-28T13:00:08-07:00",
  "entity_ids": ["ent_nvda"],
  "instrument_ids": ["ins_us_nvda"],
  "fiscal_period": "FY2026Q1",
  "license_policy": "TRAIN_ALLOWED",
  "sha256": "...",
  "raw_uri": "s3://raw-data/.../original.pdf",
  "processing_state": "RECEIVED"
}
```

示例时间仅展示字段格式，不声明真实发布日期。

### 4.4 Intake 质量门

资产满足以下条件才进入 Processing：文件可读、哈希完成、来源已登记、发布时间及精度已声明、授权策略存在、必要实体或数据范围存在。缺失授权、损坏、恶意文件、来源不明或疑似重复修订冲突的资产进入 `QUARANTINED`，不得自动进入训练链路。

## 5. 统一时间模型与历史可回放

所有语料，尤其财报、研报、新闻和访谈，必须严格标注信息发布时间。时间字段不能只保留一个模糊的 `date`。

| 字段 | 含义 |
|---|---|
| `event_time` | 现实事件发生或电话会开始时间 |
| `period_start / period_end` | 指标覆盖的财政期、观察期或预测期 |
| `source_publish_time` | 来源方正式发布时间 |
| `first_seen_time` | 本系统或指定供应商首次观察到的时间 |
| `ingest_time` | 数据进入本平台的时间 |
| `processed_time` | 某处理版本完成时间 |
| `available_time` | 在特定数据许可和系统条件下最早可被使用的时间 |
| `revision_time` | 来源修订版发布时间 |
| `timezone` | IANA 时区，如 `America/New_York` |
| `timestamp_precision` | YEAR、MONTH、DAY、MINUTE、SECOND、MILLISECOND |

约束：

- 只有发布日期时，不虚构 `00:00:00` 为精确发布时间；保存日期、时区和 `precision=DAY`。
- Earnings 事件额外记录 `PRE_MARKET`、`REGULAR_SESSION` 或 `AFTER_HOURS`。
- 文档的 section、chunk、table、utterance 必须继承来源、发布时间、可用时间和授权；可增加更精确的局部时间。
- 历史查询、数据集编译和回测强制执行 `available_time <= as_of_time`。
- 宏观和一致预期必须保存 vintage/snapshot；修订值不覆盖首次发布值。

## 6. DATA PROCESSING：解析与标准化

Processing 的职责是回答“机器从原始材料中解析出了什么”，不负责判断观点是否正确，也不直接生成训练答案。

### 6.1 标准处理流水线

```text
RAW asset
  → 格式识别与安全解包
  → 文本 / 表格 / 音频解析
  → 版面与文档结构恢复
  → 语言识别与必要的 OCR / ASR
  → 语义切分
  → 实体、指标、事件候选抽取
  → 单位、币种、期间、时区标准化
  → 自动质量检查
  → PROCESSED DATA + CANDIDATE OBJECTS + QUALITY REPORT + LINEAGE
```

### 6.2 按介质的处理

| 输入 | 处理 | 输出 |
|---|---|---|
| PDF/HTML/DOCX | 原生文本解析、OCR、阅读顺序、标题层级、页码 | section、paragraph、chunk、坐标 |
| XBRL/财务表格 | taxonomy 映射、表头展开、单位/期间解析 | 标准化 metric observation |
| XLSX/CSV/JSON | schema 映射、类型检查、缺失值和主键校验 | Lakehouse 标准表 |
| 音频/视频 | ASR、说话人分离、时间对齐、术语纠错 | utterance + speaker + audio span |
| Transcript | 角色识别、prepared remarks/Q&A 分段、与音频对齐 | utterance + event structure |
| 行情时序 | 交易日历、企业行动、时区、重复 tick 和异常价检查 | 标准 bars/ticks 表 |
| 新闻流 | 正文提取、重复稿/更新稿关联、实体和事件候选 | article + event candidates |

### 6.3 标准化规则

- **实体**：文本别名映射到内部 `entity_id`，证券映射到独立 `instrument_id`；公司与股票不是同一个 ID。
- **指标**：映射到版本化 Metric Taxonomy，如 `revenue`、`net_income_gaap`、`capex_cash_paid`。
- **数值**：原始字符串和标准化数值同时保留；单位、缩放、币种和汇率处理显式记录。
- **期间**：明确 fiscal/calendar、季度/年度、TTM、point-in-time/period-flow。
- **内容结构**：按标题、段落、表格、问答、说话人进行语义切分，不按固定字符盲切。
- **衍生值**：必须带公式、输入 observation、代码版本和 `value_type=DERIVED_*`。

### 6.4 Processing 数据结构

```json
{
  "processed_object_id": "pobj_01J...",
  "asset_id": "ast_01J...",
  "processor": "financial-pdf-parser",
  "processor_version": "2.3.1",
  "schema_version": "financial-document/1.2",
  "content_type": "TABLE_CELL",
  "text_original": "$100 billion",
  "text_normalized": "100000000000 USD",
  "evidence_locator": {
    "page": 7,
    "table_id": "tbl_03",
    "row": 4,
    "column": 2,
    "bbox": [112, 418, 256, 446]
  },
  "processed_time": "2026-08-05T11:04:12+08:00",
  "quality_flags": []
}
```

每次重跑生成新的 `processing_run_id`，旧结果不覆盖。Processing Lineage 记录输入资产、处理器版本、配置、代码 commit、运行时间和输出对象。

### 6.5 自动质量检查

- 页数、字符数、表格数和 OCR 置信度是否合理；
- 财务表合计、单位、币种、期间是否自洽；
- Transcript 是否缺段、说话人是否异常、音频时间是否连续；
- 行情是否符合交易日历、主键是否重复、价格是否存在断层；
- 实体和证券映射是否唯一；
- 处理结果是否仍携带来源、时间和授权信息。

质量报告决定对象进入自动标注、人工复核或重新处理队列。

## 7. DATA ANNOTATION：金融事实与语义标注

### 7.1 标注层级

| 层级 | 标注内容 | 示例 |
|---|---|---|
| Entity | 公司、人物、机构、证券、产品、地区 | NVIDIA、CEO、H100 |
| Fact / Metric | 指标、值、单位、币种、期间、口径 | Revenue = USD 100bn, FY2026Q1, GAAP |
| Event | 事件类型、参与方、时间、影响对象 | Earnings release、M&A、FOMC |
| Content Role | 信息的性质 | REPORTED_FACT、ANALYST_FORECAST |
| Research Semantics | 论点、催化剂、风险、假设、估值、证据 | AI demand is a growth driver |
| Relationship | 对象间可审计关系 | forecast-for、supports、contradicts、revises |

### 7.2 内容角色是强制标签

```text
REPORTED_FACT             已正式披露的事实
MANAGEMENT_COMMENTARY     管理层解释或观点
MANAGEMENT_GUIDANCE       管理层对未来的量化或定性指引
ANALYST_FORECAST          特定分析师/机构预测
ANALYST_OPINION           分析师判断、评级、投资观点
CONSENSUS_EXPECTATION     指定供应商在特定时点的一致预期
MARKET_OBSERVATION        可直接观察的行情或市场状态
DERIVED_ANALYSIS          使用明确公式或方法派生的结论
```

### 7.3 标注工作台

建设统一 **Annotation Workbench**：左侧显示原文、PDF 页面、表格或音频波形；右侧显示机器预标签；中间可框选证据并编辑结构化字段。工作流为：

```text
机器预标注
  → 一级审核员确认/修正
  → 规则校验
  → 高风险或冲突项交金融专家仲裁
  → APPROVED
  → 进入 CURATED DATA 逻辑视图
```

系统保存每次操作的 annotator、时间、guideline 版本、旧值、新值、置信度和原因。审核不覆盖历史标注，而是生成新 annotation revision。

### 7.4 Evidence-first 标注

每个标签必须绑定证据：

- PDF：`asset_id + page + table/cell 或 bbox`；
- Transcript：`utterance_id + speaker + character span`；
- 音频：`audio_asset_id + start_ms + end_ms`；
- API：`provider + endpoint + request_id + response path + event timestamp`；
- 时序：`series_id + timestamp + vendor record id`。

没有证据定位的关键财务事实不得批准。对外不可展示原文的授权材料仍可保存受控 locator，但必须遵守展示和训练许可。

### 7.5 NVDA 虚构示例

以下数字仅用于解释数据结构，不代表 NVIDIA 真实财务数据或预测：

```text
公司财报（FY2026 Q1 已报告）
Revenue = USD 100bn
Net Income = USD 50bn
Capex = USD 200bn

Broker X 研报（FY2027 Q1 预测）
Revenue = USD 120bn
Net Income = USD 60bn
Capex = USD 250bn
```

系统保存六条独立 observation，而不是三行“最新值”：

| Observation | 指标 | 目标期 | 值 | Value Type | 来源角色 |
|---|---|---|---:|---|---|
| obs-01 | Revenue | FY2026Q1 | USD 100bn | REPORTED | REPORTED_FACT |
| obs-02 | Net Income | FY2026Q1 | USD 50bn | REPORTED | REPORTED_FACT |
| obs-03 | Capex | FY2026Q1 | USD 200bn | REPORTED 或 DERIVED_REPORTED | REPORTED_FACT |
| obs-04 | Revenue | FY2027Q1 | USD 120bn | ANALYST_FORECAST | ANALYST_FORECAST |
| obs-05 | Net Income | FY2027Q1 | USD 60bn | ANALYST_FORECAST | ANALYST_FORECAST |
| obs-06 | Capex | FY2027Q1 | USD 250bn | ANALYST_FORECAST | ANALYST_FORECAST |

单条 observation 的结构示意：

```json
{
  "observation_id": "obs-04",
  "entity_id": "ent_nvda",
  "metric_id": "metric_revenue",
  "value_original": "Revenue: $120bn",
  "value_normalized": 120000000000,
  "currency": "USD",
  "unit_scale": 1,
  "fiscal_period": "FY2027Q1",
  "period_basis": "FISCAL",
  "accounting_basis": "GAAP_OR_SOURCE_SPECIFIED",
  "value_type": "ANALYST_FORECAST",
  "content_role": "ANALYST_FORECAST",
  "as_of_time": "研报正式发布时间",
  "source_asset_id": "ast_broker_x_report",
  "evidence_locator": {"page": 12, "table_id": "forecast_table", "row": 3},
  "annotation_status": "APPROVED"
}
```

特别规则：

- `FY2026Q1` 与 `FY2027Q1` 不能因名称相似而合并；财政季度必须映射到实际起止日。
- 财报中的 Capex 若不是直接披露，而是从 “purchases of property and equipment” 映射，应标为 `DERIVED_REPORTED`，并保存映射规则。
- GAAP 与 Non-GAAP、reported currency 与 converted currency 必须分开。
- 后续研报的新预测生成新 observation，并以 `REVISES` 指向旧预测，不覆盖。
- 可以计算 `forecast_growth = 20%`，但该值属于可复现的 `DERIVED_ANALYSIS`，必须保留公式和两个输入 observation。

## 8. 存储、索引与检索架构

### 8.1 推荐组件

```text
S3 / MinIO Object Store
  ├─ raw-data/          原始文件，WORM/版本控制
  ├─ processed-data/    解析文本、页面、音频片段等
  └─ datasets/          编译后的不可变数据集

PostgreSQL
  ├─ Data Asset Catalog
  ├─ Entity / Instrument Master
  ├─ Annotation Store
  ├─ License / Access Policy
  └─ Lineage / Review Audit

Lakehouse (Iceberg/Delta + Parquet)
  ├─ 财务 observation
  ├─ 行情和宏观时序
  ├─ 文本 chunk 元数据
  └─ 数据集快照与统计

OpenSearch
  ├─ 全文检索
  ├─ 结构化条件过滤
  └─ 高亮与证据定位

Vector Index（可选）
  └─ 在权限过滤后的语义召回
```

PostgreSQL 不是用来存大 PDF，S3 不是业务查询数据库，向量库也不是唯一事实源。向量索引中的每条记录必须保留 `chunk_id`，回链 Catalog 与原证据。

### 8.2 为什么按存入日期存储仍能快速找到数据

物理路径按 `ingest_date/batch_id/asset_id` 保证写入稳定、易扩展；用户通过 Catalog 和索引查询：

```text
entity = NVIDIA
AND event_type = EARNINGS
AND fiscal_period = FY2026Q1
AND document_type IN (EARNINGS_RELEASE, CALL_TRANSCRIPT, COMPANY_RESEARCH)
AND source_publish_time BETWEEN ...
AND available_time <= 查询时点
AND license_policy 允许当前用途
```

常用组合索引包括：

- `(entity_id, source_publish_time DESC)`；
- `(event_id, document_type)`；
- `(metric_id, entity_id, period_end, value_type, as_of_time)`；
- `(available_time, license_policy, quality_tier)`；
- 全文 BM25 + entity/event/time filters；
- 向量相似度 + 相同的权限和时间过滤。

所有检索结果显示来源、发布日期、内容角色和证据按钮，不能只返回脱离来源的文本。

## 9. DATASET CURATION & COMPILATION

数据集编排的职责是把已批准数据转换为某个明确训练或评估目标的不可变产物，而不是把数据库整体导出。

### 9.1 数据集类型

| 数据集 | 目的 | 典型输入 | 典型输出 |
|---|---|---|---|
| CPT / DAPT | 学习金融语言、文档结构和领域知识 | 获准语料、表格序列化文本 | tokenized corpus |
| SFT | 学习问答、抽取、总结和研究任务 | 证据 + 人工/规则构造指令答案 | messages JSONL |
| Financial Reasoning | 学习可验证计算和跨文档推理 | metric observations、事件、公式 | 问题、步骤、答案、证据 |
| Tool-use | 学习何时查询数据库/API及如何调用 | 工具 schema + 时点化任务 | tool call trajectories |
| Preference | 对齐答案质量、引用和拒答行为 | 成对或排序答案 | chosen/rejected |
| Evaluation | 独立衡量抽取、时效、推理和引用 | 严格隔离的事件/实体/时间 | prompts + hidden labels |

动态事实如最新股价、最新财报、最新一致预期应主要通过工具调用或 RAG 获取，而不是要求模型权重永久记忆。训练重点应包括选择正确来源、指定 `as_of_time`、生成合法查询并引用证据。

### 9.2 编译工作流

```text
Dataset Specification
  → Eligibility Filter
  → Curated Snapshot
  → Sampling Plan
  → Dataset Compiler
  → Schema / Finance Validation
  → Exact + Near Deduplication
  → Event-cluster Split
  → Human Review
  → Dataset Registry
```

#### A. Dataset Specification

在运行前声明：目标任务、目标模型、数据类型、允许来源、授权用途、时间截止点、语言、质量阈值、采样比例、输出 schema、split 策略和验收标准。

#### B. Eligibility Filter

只允许满足以下条件的对象进入候选集：

```text
annotation_status = APPROVED（需要标注的任务）
quality_tier IN (A, B)
license_policy 允许目标用途
available_time <= dataset_cutoff_time
source_publish_time 非空且 precision 合规
evidence_locator 完整
未被撤回、隔离或判定为污染
```

#### C. Curated Snapshot

固定 Catalog、Lakehouse 表、标注版本和知识分类版本，生成不可变 `snapshot_id`。以后原数据有更新，也不改变已经发布的数据集。

#### D. Sampling

按来源、时间、实体、行业、文档类型、语言、难度和内容角色分层，防止高频新闻或热门公司淹没整个数据集。低频但重要的金融事件可加权，权重与理由写入 Manifest。

#### E. Dataset Compiler

编译器是版本化代码，不是一次性 SQL。它将 approved objects 转换成标准训练 schema，并在每条样本保留内部追溯字段：

```json
{
  "sample_id": "smp_01J...",
  "task_type": "metric_extraction",
  "messages": [
    {"role": "user", "content": "..."},
    {"role": "assistant", "content": "..."}
  ],
  "provenance": {
    "asset_ids": ["ast_..."],
    "processed_object_ids": ["pobj_..."],
    "annotation_ids": ["ann_..."],
    "evidence_locators": [{"page": 7, "table_id": "tbl_03"}],
    "source_publish_time": "...",
    "available_time": "..."
  },
  "license_tags": ["TRAIN_ALLOWED"],
  "split_group_id": "evt_nvda_fy2026q1_earnings"
}
```

若发布给训练框架的版本不应暴露内部 URI，可把 provenance 分离到受控 sidecar；但内部 Registry 必须能从 `sample_id` 完整回溯。

#### F. 验证与去重

- schema、token 长度、编码、空答案和格式验证；
- 数值、单位、币种、财政期、公式重算；
- 引文是否真的支持答案；
- exact hash、MinHash/LSH、embedding 近重复检测；
- 同一新闻转载、研报摘要与原研报、财报与其摘录建立内容簇；
- PII、恶意指令、数据投毒和授权文本泄漏检查。

#### G. Split：先按事件簇切分，再生成样本

同一 Earnings 事件衍生出的财报、Transcript、研报、新闻、问题和改写必须进入同一个 split。正确顺序是：

```text
资产和事件建簇 → 分配 train/validation/test → 各 split 内生成样本
```

不能先生成问题再随机按行切分，否则近乎相同的上下文和答案会泄漏到测试集。评估集还应组合使用：

- **Temporal holdout**：较晚时段仅用于测试；
- **Entity holdout**：测试包含训练未见或低曝光实体；
- **Event holdout**：整个事件簇隔离；
- **Source holdout**：验证跨来源泛化；
- **Adversarial set**：期间、口径、事实/预测混淆和错误引用陷阱。

评估标准答案与评分逻辑存放在独立权限域，训练任务账户不可读取。

### 9.3 Dataset Manifest

每个发布版本必须包含：

```yaml
dataset_id: fin-sft-metric-extraction
version: 1.0.0
snapshot_id: snap_01J...
created_at: 2026-08-05T18:00:00+08:00
cutoff_time: 2026-07-31T23:59:59Z
objective: financial metric extraction with evidence
sources: [sec_filings, company_ir]
license_policies: [TRAIN_ALLOWED]
taxonomy_versions:
  entity: entity-master/3.2
  metric: financial-metrics/2.1
  annotation_guideline: annotation/1.4
compiler:
  version: 1.8.0
  git_commit: abc1234
split_strategy: event_cluster_then_temporal_holdout
counts:
  train: 0
  validation: 0
  test: 0
quality_report_uri: s3://datasets/.../quality-report.json
leakage_report_uri: s3://datasets/.../leakage-report.json
```

示例计数为占位值。Manifest 还应记录过滤条件、拒绝数量及原因、采样权重、字段 schema、tokenizer、去重阈值、人工抽检结果和已知限制。

## 10. 端到端 NVDA 示例

假设某天员工提交：NVDA 财报 PDF、Earnings Call 音频和 Transcript、一份公司研报、一份半导体行业研报、一份宏观研报；API 同时输入价量数据。

| 阶段 | 系统动作 | 人工动作 | 输出 |
|---|---|---|---|
| Intake | 哈希、安全扫描、分类建议、写 RAW、登记 Catalog | 批量确认文档类型、来源、发布时间、授权、主体 | 6 个文档资产 + 1 个 API batch |
| Processing | PDF/表格解析，ASR/说话人对齐，研报分节，行情 schema 标准化 | 仅处理异常队列 | processed objects、候选指标/事件、质量报告 |
| Annotation | 预标 Revenue、指引、预测、观点、催化剂和证据 | 审核员确认；冲突交专家 | approved annotations、curated view |
| Curation | 按用途、授权、时间截点筛选；按 earnings event 建簇；编译样本 | 抽检证据和难例 | 版本化 SFT/tool-use/eval 数据集 |

关键对象关系：

```text
NVDA entity
  ├─ has_instrument → NVDA.O instrument
  └─ participates_in → FY2026Q1 earnings event
                         ├─ evidenced_by → earnings release asset
                         ├─ evidenced_by → call audio/transcript assets
                         ├─ discussed_by → company research asset
                         └─ observed_after → price/volume series
```

模型训练样本如果询问“截至财报发布时，公司报告的 Revenue 是多少”，答案只能引用财报 observation；若询问“Broker X 对下一财季 Revenue 的预测是多少”，答案只能引用对应研报 prediction。跨来源推理可以比较二者，但必须保留各自期间、发布时间和证据。

## 11. 工具模块设计

| 模块 | 核心能力 | 主要用户 |
|---|---|---|
| Intake Portal | 批量上传、预分类、必填校验、授权选择 | 数据运营 |
| Connector Manager | API/SFTP 配置、调度、schema 和故障重试 | 数据工程 |
| Data Asset Catalog | 资产搜索、版本、来源、时间、授权、血缘 | 全团队 |
| Processing Orchestrator | DAG、解析器、重跑、质量报告 | 数据工程 |
| Annotation Workbench | 证据框选、预标签、双审、专家仲裁 | 标注员/金融专家 |
| Taxonomy Manager | 实体、指标、事件、行业、主题词表版本管理 | 数据治理 |
| Dataset Builder | specification、筛选、采样、编译、split | AI/数据团队 |
| Dataset Registry | 版本、Manifest、审批、下载、下线 | AI/治理团队 |
| Governance Console | RBAC、许可、审计、保留期、删除传播 | 安全/法务 |

建议统一使用一个 Web 门户提供任务入口，但后端保持模块化服务和稳定 ID 契约，避免把所有逻辑写进单体上传页面。

## 12. 权限、授权与删除传播

- 采用 RBAC + ABAC：用户角色、数据敏感度、来源许可、用途、地域共同决定访问。
- 授权策略必须机器可执行，不只保存在合同 PDF；过期后自动阻止新的处理、检索和数据集编译。
- 每次导出记录用户、用途、数据集版本、来源范围和审批记录。
- 对受限研报保存原文件时，全文展示、训练、向量化、摘要和外发可能是不同权限，必须分别建 policy。
- 收到撤回、纠错或删除要求时，通过 lineage 找到所有 processed objects、annotations、indexes、dataset samples 和 checkpoints 的影响范围，执行下线或重训决策。
- 公开仓库和日志中不得出现供应商原文、密钥、内部 URI 或未获公开许可的数据。

## 13. 数据质量指标与运营看板

| 阶段 | 核心指标 |
|---|---|
| Intake | 接收成功率、重复率、来源/授权/时间字段完整率、隔离率 |
| Processing | 解析成功率、OCR/ASR 置信度、表格准确率、处理延迟、重跑率 |
| Annotation | 预标采纳率、审核一致率、专家仲裁率、证据完整率、每项耗时 |
| Curation | 合格率、来源/实体/时间分布、重复率、泄漏率、抽检通过率 |
| Governance | 未授权访问阻断、许可即将过期、删除传播完成率、审计覆盖率 |

除平均值外，应按来源、文档类型、语言和处理器版本分层观察；整体高准确率可能掩盖某一种重要财报表格持续失败。

## 14. 团队职责

| 角色 | 责任 |
|---|---|
| Data Operations | 数据接收、来源登记、基础分类、异常跟进 |
| Data Engineering | 连接器、存储、schema、处理流水线、检索和可靠性 |
| Financial Taxonomy / SME | 指标口径、事件定义、标注规范、冲突仲裁 |
| Annotation Operations | 按规范审核预标签、绑定证据、质量抽检 |
| ML / AI Engineering | 数据集 specification、编译器、训练接口、数据评估 |
| Legal / Compliance | 来源合同、允许用途、保留和删除政策 |
| Data Governance Owner | ID、版本、质量门、发布审批和审计制度 |

## 15. 分阶段实施计划

### Phase 0：标准先行（2-3 周）

- 冻结首版 Asset、Entity、Metric、Event、Annotation 和 Dataset schema；
- 建立来源注册表、授权枚举、时间标准和内容角色规范；
- 选择 3 类试点：公司财报、Earnings Call、卖方研报；
- 以 NVDA 等 3-5 个公司事件做 gold samples 和验收集。

### Phase 1：可用闭环（4-6 周）

- S3/MinIO RAW Store、PostgreSQL Catalog、员工批量上传门户；
- PDF/Transcript 基础解析和证据定位；
- Annotation Workbench MVP；
- 资产搜索与一个 SFT Dataset Compiler；
- lineage、Manifest、基础 RBAC 和审计日志。

验收：任意训练样本能在两次点击或一个 API 调用内回溯到原始页/表格/语句；来源、时间、授权完整率 100%。

### Phase 2：规模化（6-10 周）

- API Connector、行情与宏观 Lakehouse；
- 音频 ASR/说话人对齐、XBRL/复杂表格解析；
- OpenSearch 与事件中心；
- 机器预标注、双审/仲裁、质量运营看板；
- 多类型 compiler、事件簇 split、泄漏检测。

### Phase 3：工业化治理（持续）

- Dataset Registry、审批和自动发布；
- 许可到期、撤回与删除传播；
- 数据漂移、来源质量和处理器回归监控；
- tool-use 数据集、时间回放环境和独立评估权限域；
- 对每次模型训练固化 dataset/model/code/config 四类版本。

## 16. 最小验收标准

系统进入正式训练前，至少满足：

1. 每个原始资产有稳定 ID、哈希、来源、授权、发布时间精度、可用时间和不可变 URI。
2. 每个 processed object 有输入资产和处理版本血缘。
3. 每个关键事实、预测和观点有明确内容角色与证据定位。
4. 任何历史查询能强制执行 `available_time <= as_of_time`。
5. 同一指标的 actual、guidance、analyst forecast、consensus 可并存且不会覆盖。
6. 数据集只从固定快照编译，包含 Manifest、质量报告和泄漏报告。
7. 同一事件簇不会跨 train/validation/test。
8. 评估答案与训练账户隔离。
9. 未授权或授权过期数据无法进入新数据集。
10. 任意 `sample_id` 可回溯到 annotation、processed object、raw asset 和具体证据。

## 17. 系统边界与下一份设计

本架构向模型训练系统交付：版本化 dataset artifact、Dataset Manifest、schema、tokenizer 要求、授权标签、质量/泄漏报告和完整 lineage。后续《模型训练与评估架构》应继续定义 CPT/SFT/tool-use/preference 的训练作业、实验追踪、checkpoint、Model Registry、评估门槛和线上调用方式，但不得绕过本文的数据权限、时间与证据约束。
